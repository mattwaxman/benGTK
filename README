README

1. Install GTK3
sudo apt-get install libgtk-3-dev

2. Install GCC
sudo apt install gcc

3. Download example-0.c file

4. Compile example-0.c
gcc `pkg-config --cflags gtk+-3.0` -o example-0 example-0.c `pkg-config --libs gtk+-3.0`
